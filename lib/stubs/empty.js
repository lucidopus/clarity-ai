// Empty stub for Node.js built-in modules that are referenced but unused in the browser.
// Used by next.config.ts turbopack.resolveAlias to prevent build errors from
// manim-web's opentype.js dependency which has `require('fs')` calls.
module.exports = {};
